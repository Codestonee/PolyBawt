# =============================================================================
# LIVE TRADING CONFIGURATION - REVOKED/DISABLED
# =============================================================================
# 
# WARNING: This configuration has been DISABLED after significant losses.
# DO NOT ENABLE live trading without:
#   1. Complete code review
#   2. Proper backtesting
#   3. Paper trading validation
#   4. Risk management verification
#
# Current Status: EMERGENCY DISABLED
# =============================================================================

environment: paper  # FORCED TO PAPER MODE
dry_run: true      # NO LIVE ORDERS

# -----------------------------------------------------------------------------
# API Endpoints
# -----------------------------------------------------------------------------
api:
  clob_base_url: "https://clob.polymarket.com"
  gamma_base_url: "https://gamma-api.polymarket.com"
  ws_url: "wss://ws-subscriptions-clob.polymarket.com/ws/market"
  polygon_chain_id: 137

# -----------------------------------------------------------------------------
# Trading Parameters - ALL DISABLED
# -----------------------------------------------------------------------------
trading:
  assets: []  # NO ASSETS - TRADING DISABLED
  bankroll: 0.58  # Remaining balance

  min_edge_threshold: 0.10  # Increased to 10%
  kelly_fraction: 0.10  # Very conservative
  max_position_pct: 0.01  # 1% max
  max_asset_exposure_pct: 0.02  # 2% max
  cutoff_seconds: 300  # 5 minutes before expiry
  min_seconds_after_open: 120  # 2 minutes after open

  favorite_bet_enabled: false
  favorite_bet_size: 0

  arbitrage_enabled: false
  arbitrage_min_profit_pct: 0.02
  arbitrage_max_size: 0

  extreme_prob_enabled: false
  extreme_prob_threshold: 0.05
  extreme_prob_kelly_boost: 1.0
  extreme_prob_min_edge: 0.05

# -----------------------------------------------------------------------------
# Risk Management
# -----------------------------------------------------------------------------
risk:
  daily_loss_soft_limit_pct: 0.01  # 1% soft stop
  daily_loss_hard_limit_pct: 0.02  # 2% hard stop
  max_drawdown_pct: 0.05  # 5% max drawdown
  volatility_pause_threshold: 1.5
  correlation_reduction_threshold: 0.7

# -----------------------------------------------------------------------------
# Execution Parameters
# -----------------------------------------------------------------------------
execution:
  rate_limit_buffer_pct: 0.20
  ws_reconnect_initial_ms: 100
  ws_reconnect_max_ms: 5000
  ws_reconnect_multiplier: 2.0
  ws_reconnect_jitter: 0.2
  order_timeout_ms: 5000
  max_retries: 2

# -----------------------------------------------------------------------------
# Market Making - DISABLED
# -----------------------------------------------------------------------------
market_making:
  enabled: false
  spread_bps: 50.0
  quote_size_usd: 0
  max_inventory_usd: 0
  skew_bps_per_inventory: 1.0
  min_profit_margin: 0.005

# -----------------------------------------------------------------------------
# Event Trading - DISABLED
# -----------------------------------------------------------------------------
event_trading:
  enabled: false
  categories: []
  min_edge_politics: 0.10
  min_edge_sports: 0.10
  min_edge_economics: 0.10
  min_edge_pop_culture: 0.10
  max_position_per_market: 0
  max_category_exposure: 0
  rebalance_enabled: false
  rebalance_threshold_pct: 0.10
  refresh_polls: 3600
  refresh_betting_lines: 900
  refresh_fed_futures: 900
  refresh_sentiment: 1800
  brier_tracking_enabled: true
  calibration_db_path: "data/calibration.db"

# -----------------------------------------------------------------------------
# VPIN - DISABLED
# -----------------------------------------------------------------------------
vpin:
  enabled: false
  bucket_size_usd: 1000.0
  n_buckets: 50
  halt_threshold: 0.5
  reduce_threshold: 0.3

# -----------------------------------------------------------------------------
# OBI Signal - DISABLED
# -----------------------------------------------------------------------------
obi:
  enabled: false
  levels: 5
  strong_threshold: 0.4
  weak_threshold: 0.2
  weighted: true

# -----------------------------------------------------------------------------
# Model Parameters
# -----------------------------------------------------------------------------
models:
  jump_intensity_per_day: 2.0
  jump_size_std: 0.02
  default_volatility:
    BTC: 0.60
    ETH: 0.70
    SOL: 0.85
    XRP: 0.90

# -----------------------------------------------------------------------------
# Observability
# -----------------------------------------------------------------------------
observability:
  log_level: INFO
  log_format: clean
  metrics_port: 9090
  telegram_enabled: false

# -----------------------------------------------------------------------------
# EMERGENCY DISABLED FLAG
# -----------------------------------------------------------------------------
# DO NOT REMOVE THIS FLAG
trading_enabled: false  # Master kill switch
